# 📋 Статус задач PigWeight

**Дата обновления:** 2025-10-14

---

## ✅ Выполнено (6/8 задач)

### 1. ✅ Исправление позиционирования всплывающих счетчиков
- **Статус:** Выполнено
- **Детали:**
  - Исправлено использование `baseX/baseY` вместо `x/y` для стационарной позиции
  - Кружок и стрелка теперь отображаются на правильной позиции линии пересечения
  - Всплывающий текст корректно анимируется вверх от базовой точки
- **Коммит:** `fix: исправлено позиционирование всплывающих счетчиков`

### 2. ✅ Синхронизация инференса с видео
- **Статус:** Выполнено
- **Детали:**
  - Улучшен мониторинг задержек между кадрами
  - Убрана блокировка на застревании видео
  - Добавлен мониторинг delta между кадрами
  - Логирование медленных кадров (> 1 сек)
- **Коммит:** `refactor: компактное логирование и улучшенная синхронизация`

### 3. ✅ Компактное логирование координат пересечений
- **Статус:** Выполнено
- **Детали:**
  - Новый формат логов:
    - `🔵 L=0.250 y=0.456 t5 ←IN (3)` - вход слева
    - `🔴 R=0.750 y=0.234 t5 →IN (2)` - вход справа
    - `🔵 L=0.250 y=0.789 t5 OUT→ (2)` - выход слева
    - `🔴 R=0.750 y=0.567 t5 ←OUT (1)` - выход справа
  - Координаты всегда точные: left=0.25, right=0.75
  - Оптимизация для токеномики и читаемости
- **Коммит:** `refactor: компактное логирование и улучшенная синхронизация`

### 4. ✅ Удаление избыточного логирования UI
- **Статус:** Выполнено
- **Детали:**
  - Удалено 122 строки избыточного debug-кода
  - Убраны логи: resize, scheduleOverlay, drawOverlay, линий
  - Оставлен только критичный лог пересечений (🎯)
  - Консоль теперь чистая и информативная
- **Коммит:** `refactor: убрано избыточное логирование в video-manager`

### 5. ✅ Фоновое журналирование событий с камер
- **Статус:** Выполнено
- **Детали:**
  - Система уже работает через `services/event_logger.py`
  - Асинхронное журналирование пересечений линий
  - Журналирование пиковых значений
  - Всплесков активности
  - Сохранение в JSONL формате для эффективности
  - Автоматическая очистка старых событий (24 часа)
- **Файлы:** `services/event_logger.py`, интеграция в `api/app.py`

### 6. ✅ Группировка событий по датам и актам
- **Статус:** Выполнено
- **Детали:**
  - API endpoint: `GET /api/events/{stream_id}/grouped`
  - Группировка по датам с подсчетом:
    - Пересечений (left/right, enter/exit)
    - Пиковых значений
    - Общего количества событий
  - Фильтрация по конкретной дате
  - Сортировка от новых к старым
- **Коммит:** `feat: добавлены API endpoints для журнала событий`

---

## 🔄 В работе (1/8 задач)

### 8. 🔄 Интеграция журнала в UI с фильтрами и экспортом
- **Статус:** В работе
- **Что сделано:**
  - API endpoints готовы:
    - `GET /api/events/{stream_id}` - получение событий
    - `GET /api/events/{stream_id}/grouped` - группировка
    - `GET /api/events/{stream_id}/export` - экспорт (JSON/CSV)
- **Что нужно:**
  - Создать UI компонент для просмотра журнала
  - Добавить фильтры (дата, тип события, поток)
  - Интегрировать кнопки экспорта
  - Визуализация группированных данных

---

## ⏳ Ожидают выполнения (2/8 задач)

### 4. ⏳ Оптимизация UI - дублирование и визуализация
- **Статус:** Ожидает
- **Требуется:**
  - Проверить дублирование компонентов в `static/`
  - Удалить устаревший код (старая система счетчиков)
  - Улучшить визуальную читаемость
  - Проверить responsive дизайн

### 7. ⏳ Сверка данных с Excel файлом (.xlsx)
- **Статус:** Ожидает
- **Требуется:**
  - Парсинг Excel файла `docs/Замеры 20.07 по 03.09.xlsx`
  - Сопоставление с данными из журнала событий
  - API endpoint для сравнения данных
  - UI для отображения расхождений
  - Экспорт отчета о сверке

---

## 📊 Общая статистика

- **Всего задач:** 8
- **Выполнено:** 6 (75%)
- **В работе:** 1 (12.5%)
- **Ожидает:** 2 (12.5%)

---

## 🚀 Следующие шаги

1. **Завершить задачу #8** - Создать UI для журнала событий
2. **Начать задачу #4** - Оптимизировать UI, убрать дублирование
3. **Начать задачу #7** - Реализовать сверку с Excel

---

## 📝 Коммиты в этой сессии

1. `90a60f0` - refactor: убрано избыточное логирование в video-manager
2. `224c96b` - refactor: компактное логирование и улучшенная синхронизация
3. `725836f` - feat: добавлены API endpoints для журнала событий

---

## 🔧 Технические детали

### Доступные API endpoints:

#### События:
- `GET /api/events/{stream_id}` - список событий
  - Параметры: `event_type`, `limit`, `since`
- `GET /api/events/{stream_id}/grouped` - группировка по датам
  - Параметры: `date` (YYYY-MM-DD)
- `GET /api/events/{stream_id}/export` - экспорт
  - Параметры: `date`, `format` (json|csv)

#### Журнал взвешиваний:
- `GET /api/journal` - список актов
- `POST /api/journal/save` - сохранение акта
- `GET /api/export/weighing` - экспорт

### Формат логов пересечений:

```
🔵 L=0.250 y=0.456 t5 ←IN (3)   # левая линия, вход, трек 5, всего 3
🔴 R=0.750 y=0.234 t5 →IN (2)   # правая линия, вход, трек 5, всего 2
🔵 L=0.250 y=0.789 t5 OUT→ (2)  # левая линия, выход, трек 5, осталось 2
🔴 R=0.750 y=0.567 t5 ←OUT (1)  # правая линия, выход, трек 5, осталось 1
```

### Структура журнала событий:

```json
{
  "event_id": "evt_1728936000000_000001",
  "stream_id": "cam1",
  "event_type": "line_crossing",
  "timestamp": 1728936000.0,
  "timestamp_str": "2024-10-14 15:20:00",
  "pig_count": 5,
  "confidence": 0.95,
  "side": "left",
  "movement": "left_to_right",
  "metadata": {
    "direction": "enter",
    "track_id": 5,
    "x": 0.25,
    "y": 0.456
  }
}
```

