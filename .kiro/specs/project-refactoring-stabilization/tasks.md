# План реализации: Стабилизация и Верификация

**Статус:** В процессе выполнения  
**Последнее обновление:** 2025-10-14

---

## ✅ ВЫПОЛНЕНО

### Фоновое журналирование и API

- [x] **Фоновое журналирование событий**
  - [x] Реализован `services/event_logger.py` для асинхронного журналирования
  - [x] Журналирование пересечений линий, пиковых значений, всплесков активности
  - [x] Сохранение в JSONL формате с автоматической очисткой (24 часа)
  - _Требования: 4.2, 4.3_

- [x] **API endpoints для журнала событий**
  - [x] Создан `api/endpoints/events.py` с endpoints:
    - `GET /api/events/{stream_id}` - получение событий
    - `GET /api/events/{stream_id}/grouped` - группировка по датам
    - `GET /api/events/{stream_id}/export` - экспорт (JSON/CSV)
  - _Требования: 2.2, 4.2_

- [x] **Сверка данных с Excel**
  - [x] Создан `services/excel_validator.py` для парсинга и анализа Excel
  - [x] Создан `api/endpoints/validation.py` с endpoints:
    - `GET /api/validation/excel/parse` - парсинг файла
    - `GET /api/validation/excel/compare` - сверка с журналом
    - `GET /api/validation/excel/report` - детальный отчет (JSON/HTML)
  - [x] Автоматическая нормализация данных и генерация отчетов
  - _Требования: 5.3_

### Улучшения UI и логирования

- [x] **Улучшения логирования и UI**
  - [x] Компактное логирование координат пересечений (формат: `🔵 L=0.250 y=0.456 t5 ←IN (3)`)
  - [x] Удалено избыточное логирование UI (-122 строки)
  - [x] Исправлено позиционирование всплывающих счетчиков
  - [x] Улучшена синхронизация инференса с видео
  - _Требования: 2.2, 2.3_

- [x] **2.2. Исправление сброса положения линий при перемотке** ✅
  - [x] `VideoStream` хранит собственные `line_positions` (строка 447 в api/app.py)
  - [x] Метод `_get_current_line_positions()` использует локальные позиции (строки 502-509)
  - [x] Endpoint `/api/stream/{stream_id}/line_positions` сохраняет позиции (строки 1514-1538, 1910-1942)
  - [x] Позиции загружаются при создании стрима (строки 1304-1314)
  - [x] Функции `load_line_positions()` и `save_line_positions()` работают с JSON файлом
  - _Требования: 2.1, 2.4_
  - **Примечание:** Эта задача была реализована ранее, но не была отмечена как выполненная

---

## 🔄 В РАБОТЕ

### 2. Исправление стабильности фронтенда

- [ ] **2.1. Диагностировать и исправить отсутствие отрисовки масок**
  - Проанализировать WebSocket сообщения на клиенте (Network -> WS -> Messages)
  - Проверить, что данные масок приходят с бэкенда
  - Исправить логику в `drawOverlay` в `static/index.html`, если она некорректно обрабатывает данные
  - _Требования: 2.2, 2.3_
  - **Статус проверки:**
    - ✅ Маски отправляются через WebSocket в `payload.debug.masks`
    - ✅ Функция `drawOverlay(masks, ids)` существует в `static/index.html`
    - ❓ Требует проверки: корректно ли обрабатываются данные в браузере

- [ ] **2.3. Создать UI компонент для журнала событий**
  - Создать интерфейс для просмотра журнала событий
  - Добавить фильтры (дата, тип события, поток)
  - Интегрировать кнопки экспорта (JSON/CSV)
  - Визуализация группированных данных по датам
  - _Требования: 2.2, 4.2_
  - **Статус:** API готово, UI не реализован

---

## 📋 СЛЕДУЮЩИЕ ЗАДАЧИ

### High Priority

- [ ] **4.1. Верифицировать и отладить `EventLogger` и `_finalize_act_to_files`**
  - Убедиться, что события пересечений и пиков корректно записываются в `records/events`
  - Проверить, что скриншоты событий сохраняются в `records/frames`
  - Проверить, что при остановке потока создаются корректные `.json`, `.svg`, и `.md` отчеты по акту
  - _Требования: 4.2, 4.3, 4.5_
  - **Статус проверки:**
    - ✅ Метод `_finalize_act_to_files()` реализован (строка 515 в api/app.py)
    - ✅ Вызывается при остановке стрима (строки 1150, 1249, 1324)
    - ❓ Требует тестирования на реальных данных

### Medium Priority

- [ ] **1.1. Оптимизировать `api/app.py`**
  - Вынести бизнес-логику в отдельные сервисы
  - Упростить роутинг (endpoints уже модуляризованы)
  - Улучшить структуру инициализации приложения
  - Удалить дублированный код
  - _Требования: 1.1, 1.2, 1.3_
  - **Текущий размер:** 2968 строк
  - **Цель:** ~1000-1500 строк
  - **Примечание:** Endpoints уже модуляризованы (12 файлов в `api/endpoints/`)

- [ ] **5.1. Написать Unit-тесты для ключевых модулей**
  - `tests/test_processor.py`: проверить логику подсчета и трекинга
  - `tests/test_event_logger.py`: проверить корректность записи событий
  - `tests/test_excel_validator.py`: проверить парсинг и сверку Excel
  - _Требования: 5.1, 5.4_

---

## 🔮 БУДУЩИЕ УЛУЧШЕНИЯ (Low Priority)

- [ ] **1.3. Создать middleware для централизованной обработки**
  - Middleware для логирования запросов
  - Middleware для обработки ошибок
  - Middleware для метрик производительности
  - _Требования: 1.1_

- [ ] **4.2. Реализовать надежный механизм retry для `av_worker`**
  - Автоматическое переподключение при сбоях
  - Graceful degradation при недоступности воркера
  - _Требования: 4.1, 4.4_

- [ ] **5.2. Реализовать интеграционные и нагрузочные тесты**
  - End-to-end тесты видеообработки
  - Нагрузочные тесты для множественных потоков
  - _Требования: 5.2, 5.3_

- [ ] **6. Дальнейшая оптимизация производительности**
  - Профилирование использования памяти
  - Оптимизация батчинга
  - Управление ресурсами
  - _Требования: 6.1, 6.2, 6.5_

- [ ] **7. Улучшения UI/UX**
  - Проверить дублирование компонентов в `static/`
  - Удалить устаревший код (старая система счетчиков)
  - Улучшить responsive дизайн
  - _Требования: 2.2, 2.3_

---

## 📊 СТАТИСТИКА

- **Всего задач:** 15
- **Выполнено:** 5 групп задач (33%)
- **В работе:** 2 задачи (13%)
- **Ожидают:** 8 задач (54%)

### Прогресс по требованиям

- **Требование 1 (API рефакторинг):** 20% (endpoints модуляризованы, app.py требует оптимизации)
- **Требование 2 (Фронтенд):** 60% (UI улучшения выполнены, линии исправлены, маски требуют проверки, UI журнала не готов)
- **Требование 3 (Типы):** 0% (не начато)
- **Требование 4 (Воркеры):** 50% (журналирование работает, требует верификации)
- **Требование 5 (Тестирование):** 0% (не начато)
- **Требование 6 (Производительность):** 0% (не начато)

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

### Созданные файлы
- `services/event_logger.py` - журналирование событий
- `services/excel_validator.py` - валидация Excel
- `api/endpoints/events.py` - API журнала событий
- `api/endpoints/validation.py` - API сверки с Excel
- `TASKS_STATUS.md` - детальный статус выполненных задач
- `SPEC_TASKS_REVIEW.md` - полная проверка всех задач

### Требуют внимания
- `api/app.py` (2968 строк) - требует оптимизации
- `static/index.html` - проверка отрисовки масок

---

## 🚀 РЕКОМЕНДУЕМЫЙ ПЛАН ДЕЙСТВИЙ

### Немедленно (HIGH)
1. ✅ Отметить задачу 2.2 как выполненную (уже реализовано)
2. 🔄 Завершить задачу 2.3 - создать UI для журнала событий
3. ⚠️ Проверить задачу 2.1 - отрисовка масок (может быть уже работает)
4. ⚠️ Протестировать задачу 4.1 - EventLogger и финализация актов

### Скоро (MEDIUM)
5. 📊 Задача 1.1 - оптимизировать api/app.py (2968 → 1500 строк)
6. 🧪 Задача 5.1 - написать unit-тесты

### Позже (LOW)
7. Middleware для централизованной обработки
8. Retry механизм для av_worker
9. Интеграционные тесты
10. Оптимизация производительности
11. UI/UX улучшения

---

## 📝 ПРИМЕЧАНИЯ

**Критические находки при проверке:**
1. ✅ **Задача 2.2 УЖЕ ВЫПОЛНЕНА** - система line_positions работает корректно
2. ⚠️ **Задача 2.1** - маски отправляются через WS, но требует проверки отрисовки
3. ⚠️ **Задача 4.1** - код реализован, требует тестирования
4. 📊 **api/app.py** - 2968 строк, много дублирования, требует рефакторинга

**API Endpoints готовы:**
- События: `/api/events/{stream_id}`, `/api/events/{stream_id}/grouped`, `/api/events/{stream_id}/export`
- Валидация: `/api/validation/excel/parse`, `/api/validation/excel/compare`, `/api/validation/excel/report`
- Линии: `/api/stream/{stream_id}/line_positions` (POST)
