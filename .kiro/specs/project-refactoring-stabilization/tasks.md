# План реализации: Стабилизация и Верификация

**Приоритет:** Исправление критических багов для верификации основной функциональности (запись пересечений и журналирование).

## High Priority (Сейчас в работе)

- [ ] **2. Исправление стабильности фронтенда**
  - [ ] **2.1. Диагностировать и исправить отсутствие отрисовки масок**
    - Проанализировать WebSocket сообщения на клиенте (Network -> WS -> Messages).
    - Проверить, что данные масок приходят с бэкенда.
    - Исправить логику в `drawOverlay` в `static/index.html`, если она некорректно обрабатывает данные.
    - _Требования: 2.2, 2.3_
  - [ ] **2.2. Исправить сброс положения линий при перемотке**
    - Изменить `api/app.py`: `VideoStream` должен хранить собственные `line_positions`.
    - Изменить эндпоинт `/api/lines`, чтобы он обновлял позиции для конкретного стрима.
    - Убрать перезапись `window.__lines` на фронтенде данными с сервера для файловых стримов.
    - _Требования: 2.1, 2.4_

- [ ] **4. Повышение надежности рабочих процессов**
  - [ ] **4.1. Верифицировать и отладить `EventLogger` и `_finalize_act_to_files`**
    - Убедиться, что события пересечений и пиков корректно записываются в `records/events`.
    - Проверить, что скриншоты событий сохраняются в `records/frames`.
    - Проверить, что при остановке потока создаются корректные `.json`, `.svg`, и `.md` отчеты по акту.
    - _Требования: 4.2, 4.3, 4.5_

## Medium Priority (Следующие шаги)

- [ ] **1. Завершение рефакторинга API**
  - [ ] **1.1. Перенести оставшиеся endpoints из `api/app.py`**
    - Перенести эндпоинты, связанные с записями (`/api/records/*`), в `api/endpoints/records.py`.
    - Перенести эндпоинты, связанные с верификацией (`/api/verification/*`), в `api/endpoints/verification.py`.
    - Удалить `api/simple_endpoints.py`, перенеся остатки логики.
    - _Требования: 1.2, 1.3_

- [ ] **5. Создание базовой системы тестирования**
  - [ ] **5.1. Написать Unit-тесты для ключевых модулей**
    - `tests/test_processor.py`: проверить логику подсчета и трекинга.
    - `tests/test_event_logger.py`: проверить корректность записи событий.
    - _Требования: 5.1, 5.4_

## Low Priority (На будущее)

- [ ] **1.3. Создать middleware для централизованной обработки**
- [ ] **4.1. Реализовать надежный механизм retry для `av_worker`**
- [ ] **5.2. Реализовать интеграционные и нагрузочные тесты**
- [ ] **6. Дальнейшая оптимизация производительности и управления ресурсами**
- [ ] **7. Улучшения UI/UX**