# 🚀 PigWeight - Универсальные профили производительности

## ✅ Универсальная оптимизация завершена!

Система теперь автоматически выбирает оптимальный рантайм и профиль производительности для любого железа.

### 🎯 Как это работает:
- **Анализ GPU**: Автоматически определяет архитектуру и возможности
- **Выбор профиля**: Умная логика на основе модели GPU и объема VRAM
- **Рантайм**: PyTorch CUDA → ONNX GPU → ONNX CPU → CPU fallback
- **Настройки**: Применяет оптимальные параметры для конкретного железа

### 📊 Доступные профили:

#### 🏆 ULTRA_PERFORMANCE
- **Для**: RTX 4080+, RTX 3070+, 10GB+ VRAM
- **FPS**: 50
- **Batch**: 12
- **Разрешение**: 960px
- **Использование**: Максимальная производительность

#### ⚡ RTX_OPTIMIZED
- **Для**: RTX среднего класса, 4-8GB VRAM
- **FPS**: 35
- **Batch**: 8  
- **Разрешение**: 832px
- **Использование**: Оптимизировано для RTX архитектуры

#### 🎯 BALANCED  
- **Для**: GTX 1060/1070/1080, Tesla, Quadro
- **FPS**: 25
- **Batch**: 6
- **Разрешение**: 768px
- **Использование**: Баланс производительности и качества

#### 💾 POWER_SAVING
- **Для**: Старые/слабые GPU, <4GB VRAM
- **FPS**: 20
- **Batch**: 4
- **Разрешение**: 640px
- **Использование**: Экономия ресурсов

#### 💻 CPU_ONLY
- **Для**: Только CPU обработка
- **FPS**: 15
- **Batch**: 2
- **Разрешение**: 640px
- **Использование**: Без GPU

### 🛠️ Способы запуска:

```bash
# Автоматический выбор рантайма и профиля (рекомендуется)
python main_optimized.py

# Ручной выбор рантайма (профиль всё равно автоматический)
python main_optimized.py --runtime pytorch
python main_optimized.py --runtime onnx-gpu
python main_optimized.py --runtime onnx-cpu

# Режим отладки
python main_optimized.py --debug

# Другой порт
python main_optimized.py --port 8080

# Помощь
python main_optimized.py --help
```

### 🔧 Логика автоматического выбора:

1. **Анализ GPU**: Определяет модель и объем VRAM
2. **Выбор профиля**: Автоматически на основе архитектуры GPU
3. **Проверка рантаймов**: PyTorch CUDA → ONNX GPU → ONNX CPU → CPU
4. **Оптимизация**: Применяет специфичные настройки

**🎯 Профили больше не нужно выбирать вручную - система всегда сама выбирает оптимальный!**

### 💡 Преимущества универсальной оптимизации:

- ✅ **Универсальность**: Работает оптимально на любом железе
- ✅ **Автоматизация**: Никаких ручных настроек
- ✅ **Умная логика**: Учитывает архитектуру и возможности GPU
- ✅ **Стабильность**: Оптимальные настройки для каждого класса GPU
- ✅ **Экономия ресурсов**: Оптимальное использование VRAM и CPU

### 📈 Примеры автоматического выбора:

#### 🔥 RTX 4090/4080 (10GB+ VRAM):
```
✅ Профиль: ULTRA_PERFORMANCE  
⚡ 50 FPS, batch 12, 960px
```

#### ⚡ RTX высокого класса (6-8GB VRAM):
```
✅ Профиль: ULTRA_PERFORMANCE
⚡ 50 FPS, batch 12, 960px
```

#### 🎯 RTX среднего класса (4GB VRAM):
```
✅ Профиль: RTX_OPTIMIZED
⚡ 35 FPS, batch 8, 832px
```

#### 💻 GTX 1070/1060 (6GB VRAM):
```
✅ Профиль: BALANCED  
⚡ 25 FPS, batch 6, 768px
```

#### 💻 Слабые GPU/CPU:
```
✅ Профиль: POWER_SAVING/CPU_ONLY
⚡ 15-20 FPS, batch 2-4, 640px
```

### 💡 Рекомендации:

1. **Просто запускайте** `python main_optimized.py` - система сама выберет всё!
2. **Никаких профилей вручную** - всё автоматически
3. **Мониторьте VRAM** - при нехватке памяти система автоматически снизит настройки
4. **Для продакшена** используйте main_optimized.py вместо main.py

Система готова к работе с полностью автоматическим выбором оптимальных настроек! 🚀