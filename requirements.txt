# Backend
fastapi==0.112.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
# Разрешаем актуальную установленную версию python-dotenv (снятый пин)
python-dotenv

# Scheduling / Timezone
APScheduler==3.10.4
tzdata>=2023.3

# Video/Imaging
# Для Python 3.13 требуются колёса numpy 2.x и совместимый OpenCV
numpy>=2.0.0,<3
opencv-python-headless>=4.10.0.84
# Стабильный декодер и seek для файлов/RTSP (низкая задержка)
av>=12.2.0
# Быстрый JPEG энкодер (опционально, но крайне желательно)
PyTurboJPEG>=1.7.6
# Фоллбек JPEG-энкодинга через Pillow
pillow>=10.0.0

# Убраны лишние зависимости клиентского инференса (norfair, rich, pygments)
